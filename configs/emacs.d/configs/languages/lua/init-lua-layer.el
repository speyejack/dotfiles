;;; Init-lua-layer.el --- Package Description
;;; Commentary:

;;; Code:

(defun jag--load-lua-layer-requires ()
  "Load each package for directory."
  (require 'init-lua-mode))

(jag--load-lua-layer-requires)

(provide 'init-lua-layer)
;;; init-lua-layer.el ends here
